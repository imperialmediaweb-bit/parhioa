{{> head title=page.title description=page.excerpt}}

<article>
  <h1>{{page.title}}</h1>
  {{#if page.featured.url}}
    <img src="{{page.featured.url}}" alt="{{page.featured.alt}}" class="featured">
  {{/if}}
  <div class="wp-content">{{{page.content}}}</div>
</article>

{{> foot}}
